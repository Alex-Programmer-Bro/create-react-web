{
  "name": "react-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "pnpm -r --filter react-web test",
    "build": "pnpm -r --filter react-web build",
    "lint": "pnpm -r --filter react-web lint",
    "typecheck": "pnpm -r --filter react-web typecheck",
    "lint-staged": "pnpm -r --filter react-web lint-staged"
  }
}
