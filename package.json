{
  "name": "create-react-web-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter create-react-web build",
    "test": "pnpm -r --filter create-react-web test",
    "lint": "pnpm -r --filter create-react-web lint",
    "typecheck": "pnpm -r --filter create-react-web typecheck",
    "lint-staged": "pnpm -r --filter create-react-web lint-staged"
  }
}
