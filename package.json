{
  "name": "create-react-command-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter create-react-command build",
    "test": "pnpm -r --filter create-react-command test",
    "lint": "pnpm -r --filter create-react-command lint",
    "typecheck": "pnpm -r --filter create-react-command typecheck",
    "lint-staged": "pnpm -r --filter create-react-command lint-staged"
  }
}
